# Q1 Ablation Table

Canonical ablation deltas are generated from `configs/ablation_delta_table.json`. The Q1 manuscript table must keep repair and reranking ablations separate from raw model ablations.

Rows with missing Vina/GNINA/RDKit coverage should be labeled as proxy-only and kept out of binding-affinity claims.
